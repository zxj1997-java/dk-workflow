server:
  port: 8080

spring:
  application:
    name: flow-example
  datasource:
    url: jdbc:mysql://localhost:3306/dk-workflow?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


# 工作流引擎配置
flow-boot:
  # 是否启用物理删除,默认为false(逻辑删除)
  physical-delete: false